package Introductory_learn.src.study.JavaSet;

public class Javaset {
    /**
     *
     * Set:
     *      元素是不允许重复的；
     *      元素是无序的；
     *
     *      HashSet:
     *       底层结构是三列节后，存储的时候根据元素的hashcode进行存储
     *          不允许元素重复
     *        原理：
     *             存放元素的时候，先进行hashcode获取，存入进入。
     *            当存放新元素的时候，先判断hashcode，如果一样，再进行equals进行比较，如果一样，就无法存入，否则可以存储
     *
     *       TreeSet:
     *          元素的比较
     *
     *
     */


}
